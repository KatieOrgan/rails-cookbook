


<div class="banner">
  <div>
    <h1>Cookbook</h1>
    <h2>Save your favourite recipes</h2>
  </div>
</div>


  <div class="card-container">
    <div class="info-top">
    <h3>List of categories</h3>
    <%= link_to 'New Category', new_category_path, class: "btn" %>
    </div>

  <div class="cards">
    <% @categories.each do |category| %>

      <%= link_to category_path(category) do %>
        <div class="card-category"
        style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)),
        url('<%= cl_image_path category.photo.key, height: 300, crop: :fill %>')">

        <h2><%= category.name %></h2>
        </div>
      <% end %>
  <% end %>



<%# <%= button_to "Create new category", request.referer || new_category_path, method: :get, class: "btn btn-secondary" %>
